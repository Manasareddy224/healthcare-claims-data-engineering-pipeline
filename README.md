# Healthcare Claims Data Engineering Pipeline

This project demonstrates an end-to-end healthcare claims data engineering pipeline using Python, PySpark, SQL-style transformations, and data quality validation checks.

The pipeline processes raw healthcare claims data through Bronze, Silver, and Gold layers and performs validation checks for completeness, accuracy, duplicates, null handling, and business rules.